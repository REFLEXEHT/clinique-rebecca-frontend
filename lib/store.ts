// DEPRECATED: Ce fichier est conservé pour compatibilité mais ne doit plus être utilisé.
// Utilisez AuthContext (context/AuthContext.tsx) pour toute la gestion d'authentification.
// L'ancien useAuthStore utilisait les clés localStorage 'token' et 'user'
// alors que AuthContext utilise 'rb_token' et 'rb_user', causant un split-brain auth.

export {}
